export const WINDOW_CONFIG = {
  windowPadding: "2.5rem",
  gapBetweenPanels: "8",
  mobileBreakpoint: 1366,
};

export const CURSOR_CONFIG = {
  trailSize: 30,
  trailSizeHover: 44,
  cursorSize: 10,
  cursorSizeClick: 7,
  trailSmoothing: 0.12,
  trailOpacity: 0.2,
  trailOpacityHover: 0.5,
};

export const ANIMATION_CONFIG = {
  windowEntryDuration: 400,
  resizeDuration: 400,
  staggerDelay: 40,
  swipeThreshold: 50,
  swipeVelocityThreshold: 0.3,
};

export const SEARCH_CONFIG = {
  maxHistory: 50,
  maxHistoryDisplay: 8,
  maxHistoryDots: 5,
  whoisTimeout: 8000,
  defaultTlds: ["com", "net", "org", "io", "dev"],
};

export const THEME_CONFIG = {
  cookieExpiry: 365,
};
